// 107-Use Multiple Conditional-Ternary-Operators.md


function checkSign(num) {
  return (num === 0) ? "num is zero" : (num > 0) ? "num is greater than zero" : "num is greater than zero";
}

checkSign(10);
